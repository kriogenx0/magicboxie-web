# HTTP-only vhost used for the first ACME HTTP-01 challenge.
server {
    listen 80;
    listen [::]:80;
    server_name d.magicboxie.com;

    include snippets/acme-challenge.conf;

    location / {
        return 404;
    }
}
