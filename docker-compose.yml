services:
  magicboxie:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./configs/magicboxie.yaml:/etc/magicboxie/config.yaml:ro
      - magicboxie-data:/var/lib/magicboxie
      - ${CONTENT_DIR:-./content}:/content
    restart: unless-stopped

volumes:
  magicboxie-data:
