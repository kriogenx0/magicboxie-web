services:
  magicbox:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./configs/magicbox.yaml:/etc/magicbox/config.yaml:ro
      - magicbox-data:/var/lib/magicbox
      - ${CONTENT_DIR:-./content}:/content
    restart: unless-stopped

volumes:
  magicbox-data:
